name: Publish Helm Chart (OCI + gh-pages)

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # Install Helm
      - name: Set up Helm
        uses: azure/setup-helm@v4

      # ✅ FIX: correct path
      - name: Get chart version
        run: |
          VERSION=$(grep '^version:' charts/fastboot-helm-chart/Chart.yaml | awk '{print $2}')
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "Chart version is $VERSION"

      # ✅ FIX: correct packaging path
      - name: Package Helm chart
        run: |
          helm package charts/fastboot-helm-chart

      # ---------------------------
      # 🟢 Push to OCI (GHCR)
      # ---------------------------
      - name: Login to GHCR
        run: echo "${{ secrets.GHCR_PAT }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push Helm chart to OCI
        run: |
          helm push fastboot-helm-chart-${{ env.VERSION }}.tgz oci://ghcr.io/fastboot-io

      # ---------------------------
      # 🔵 Publish to gh-pages
      # ---------------------------
      - name: Run chart-releaser
        uses: helm/chart-releaser-action@v1.6.0
        with:
          charts_dir: charts
          skip_existing: false
        env:
          CR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
